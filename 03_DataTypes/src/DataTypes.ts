// Task 1:
// Declare a variable with "string" dataType. Add comment about your variable

// Declare a variable with datatype string: 
let myFavColor : string = "black";
// print it on a terminal
console.log(myFavColor);

// Task 2:
// Declare a variable with "Number" dataType. Add comment about your variable

// Declare a variable with datatype Number: 
let myAge : number = 26;
// print it on a terminal
console.log(myAge);

// Task 3:
// Declare a variable with "Boolean" dataType. Add comment about your variable

// Declare a variable with datatype Boolean: 
let isUser : Boolean = true;
// print it on a terminal
console.log(isUser);

// Task 4:
// Declare a variable with "Any" dataType. Add comment about your variable

// Declare a variable with datatype Any: 
let  userName : any = "hira";
 userName = 26;
// print it on a terminal
console.log(userName);

//  Task 5:
//  Declare a variable with "let". Its value should be "undefined". Add a comment about your variable.

// Declare a variable with datatype undefined: 
let  babyName : undefined = undefined ;
// print it on a terminal
console.log(babyName);

//  Task 6:
// Declare a variable with let. It will be of type unknown. Add a comment about your variable."

// Declare a variable with datatype unknown: 
let friendName: unknown = "hira"; // unknown type is similar as any
// we store number type data here
friendName = 40;

let myFriendName =  friendName as number
myFriendName = 20;
// print it on a terminal
console.log(myFriendName);

// Task 7:
// Declare a variable with let. It value will be bigint. Add a comment about your variable."

// Declare a variable with datatype bigint: 
let  bigValue : bigint = 25221252n;
// print it on a terminal
 console.log(bigValue);

//  Task 8:
// Declare a variable with "let". Its value will be a symbol. Add a comment about your variable.

// Declare a variable with datatype symbol:
let sym1 = Symbol("52");
let sym2 = Symbol("52");
// print it on a terminal
console.log(sym1 === sym2);

// Task 9:
// Declare a variable with "let". Its value will be "null". Add a comment about your variable.

// Declare a variable with datatype null: 
let  singleValue : null = null ;
// print it on a terminal
console.log(singleValue);